{
  "name": "agent-community",
  "version": "0.0.0",
  "private": true,
  "description": "Offline scaffold for an open-source agent-native community platform",
  "type": "module",
  "license": "MIT",
  "engines": { "node": ">=24 <25" },
  "scripts": {
    "demo": "node apps/demo/main.js",
    "test": "node --test",
    "check": "node --test"
  }
}
